name: seestar_env # Garder le même nom pour remplacer l'ancien
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12    # <<< Essayer de fixer à 3.11
  - pip
  # --- Dépendances Conda de base ---
  - numpy<2.0
  - astropy>=5.0
  - scipy
  - matplotlib
  - pillow        # Nom conda pour Pillow
  - opencv        # <<< Changer python-opencv en opencv
  - psutil
  # --- Drizzle via Drizzlepac ---
  #- drizzlepac    # Laisser ici, conda devrait le trouver pour Py 3.11
  # --- Dépendances Pip ---
  - pip:
      - astroalign>=1.0
      - tqdm
      - ccdproc
      # - cupy-cudaXXX