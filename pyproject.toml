[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ZeSeestarStacker"
dynamic = ["version"]
description = "The little stacker for lots of lights."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "Tinystork" },
]
keywords = ["astronomy", "astrophotography", "stacking", "seestar", "fits"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Astronomy",
]
dependencies = [
    "numpy",
    "scipy",
    "astropy",
    "astroquery",
    "astroalign",
    "opencv-python-headless",
    "Pillow",
    "scikit-image",
    "colour-demosaicing",
    "ccdproc",
    "photutils",
    "matplotlib",
    "psutil",
    "packaging",
    "reproject",
    "tqdm",
    "drizzle",
    "bottleneck",
    "threadpoolctl",
    "PySide6",
]

[project.optional-dependencies]
tools = [
    # Standalone visu.py image viewer (seestar/tools/visu.py)
    "PyQt5",
]
qt = [
    # Compatibility alias: PySide6 is now a standard dependency (official Qt shell).
    "PySide6",
]

[project.gui-scripts]
zeseestarstacker = "seestar.qt_main:main"

[tool.setuptools.dynamic]
version = { attr = "seestar.__version__" }

[tool.setuptools.packages.find]
include = ["seestar*"]

[tool.setuptools.package-data]
seestar = ["icon/*.png"]

[tool.pytest.ini_options]
testpaths = ["tests"]
