# ZSSS 8.4.0 — Windows witness instructions (incident + W80 A/B/C/D)

Mission zsss-840-prew80-20260907 · 8.4.0 pre-W80 closure · 2026-09-07

## Status

All functional stages of the 8.4.0 memory/reliability closure, including the
final no-full-host CPU spatial rework, are committed locally on branch `beta`.
Use the exact current `beta` HEAD accepted in the final handoff report; never
use the older intermediate `c1e470f` candidate. The remaining gates are the
PHYSICAL Windows witnesses below — they are NOT claimed executed here.

No push / merge / tag / release. Reproject Phase 2 remains LOCKED until W80
acceptance.

## Code basis

- Branch `beta`, final candidate = the exact current HEAD. Record it before
  every physical run with `git rev-parse HEAD` and verify it matches Junior's
  final acceptance report. This dynamic rule deliberately prevents this
  committed document from embedding its own necessarily-stale parent SHA.
- Memory policy default is **AUTO** — there is NO "HQ RAM Limit" control in the
  normal GUI anymore. The engine resolves the CPU memory budget at execution
  from the actual machine/workload state (expert override is ONLY via the
  explicit env `ZSSS_CPU_MEMORY_OVERRIDE_BYTES`, never a GUI knob).
- Memory/GPU provenance is emitted to the durable run log: `MEMORY_POLICY`
  (per run) and `CPU_WINSOR_MEMORY_DECISION` / `_RETRY` / `_REFUSAL` (per CPU
  execution). Preserve it with each run.

## Witness 1 — user-incident (memory) witness (§72–73)

Reproduce a workload similar to the reported incident with the normal product
configuration:

- Memory policy = AUTO (no HQ RAM value chosen by the user).
- A scientific batch near `N = 36` (or the incident's own N).
- Acceptable outcomes are exactly: `FULL_CPU`, `SPATIAL_TILED_CPU`, or a
  truthful `CPU_MEMORY_REFUSAL` if genuinely impossible — never
  "outer policy accepts then a hidden 2 GiB guard rejects", never a reduced N,
  never an empty success.

Verify on disk (fresh copies):

1. failed-batch FITS remain in their input location;
2. they are NOT moved into `stacked/`;
3. logs / batch-count / checkpoint / resume state tell the same truth
   (committed count reflects only committed batches; run status FAILED).

## Witness 2 — W80 A/B/C/D normalization matrix (§74–76)

Dataset `D:\tulip\W80_v1` (IMMUTABLE master; 80 FITS: 40 ALTAZ 20 s + 40 EQ
20 s). Always copy the whole set into a fresh folder per run (e.g.
`D:\tulip\W80_runs\A` … `D`) so move-to-stacked never touches the master.

Pinned reference for EVERY run (do not let automatic selection vary):
`ALTAZLight_SH 2- 101_20.0s_LP_20241028-195131 (1268).fit`

Fixed scientific settings (identical across A/B/C/D except normalize_method):
- stacking mode / reducer: `winsorized-sigma-clip`
- batch size: `20`
- kappa: `3` / `3`
- winsor: `0.05` / `0.05`
- reference: manually pinned (1268).fit
- memory policy: AUTO (same for all runs)

| Run | normalize_method | code basis |
|-----|------------------|-----------|
| A   | sky_mean (legacy full-frame) | clean checkout of the pre-Phase-1 base `ede9d98` (8.3.0), or the legacy baseline build |
| B   | none | final accepted beta HEAD (none is a strict no-op) |
| C   | sky_mean (support-aware) | final accepted beta HEAD |
| D   | linear_fit (support-aware) | final accepted beta HEAD |

W80 is primarily a NORMALIZATION witness: with N=20 the reduction should select
`FULL_CPU`, so the new memory subsystem is effectively invisible scientifically
— do NOT force spatial tiling during the main A/B/C/D comparison (use separate
tests to exercise forced spatial CPU tiling).

## Evidence to preserve per run

- final SCI FITS + WHT / support outputs
- `normalization_diagnostics_<token>.jsonl` for C and D (real FITS identity,
  bounded allowlisted records)
- memory-policy provenance: `MEMORY_POLICY` + any `CPU_WINSOR_MEMORY_*` lines
  from the durable run log
- `run_config` / main log / registration diagnostics
- reference identity (pinned path) + Git SHA actually run
- the output of `git rev-parse HEAD` (must match the final acceptance report)

## Recommended numerical analysis (unchanged from Phase-1 guidance)

Measure background in multiple sky/background regions (not one whole-frame
mean): background median by coverage zone, spread between zones, large-scale
residual map, and correlation of background level vs footprint/coverage.
Expected: C and D strongly reduce the artificial `background ↔ geometric
coverage` dependency vs A, without flattening genuine structure. Do not claim
ALTAZ/EQ visually solved until these numbers + a visual review agree.
