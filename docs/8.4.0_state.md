# ZSSS 8.4.0 pre-W80 state

Mission: `zsss-840-prew80-20260907`
Branch: beta. Initial HEAD: ede9d98ba46aa8bf2075e50b5719293d01f659a0.
Accepted uncommitted Phase1 worktree preserved (11-file copy + SHA256 outside repo).

## Scope and invariant

User chooses science; ZSSS chooses execution. Memory never changes scientific N.
Inputs consumed only after scientific contribution committed. Phase2 Reproject
LOCKED until 8.4.0 automated closure and Windows W80 A/B/C/D human acceptance.
No publication authorized. Local commits authorized after Junior verification.

## Status

- [x] Independent archaeology and executable old-defect witnesses documented.
- [x] Fresh 236-test Phase1 closure baseline:236 passed/0failed/70.26s; full attempt interrupted
      by tool600s timeout after >75%, no process/JUnit survives. Historical35
      failures retained explicitly; complete final suite remains mandatory.
- [x] A — implemented by Coco (awaiting independent acceptance; NOT mission closure):
      per-run bounded fail-open Phase1 normalization JSONL / real FITS identity.
      Evidence below in "Stage A evidence (Coco r0)".
- [ ] B — isolated RSS profile matrix; conservative model; pure CPU memory planner.
- [x] C — implemented by Coco (awaiting independent acceptance; NOT mission closure):
      explicit budget contract, canonical exact-N spatial CPU Winsorized driver.
      Evidence below in "Stage C evidence (Coco r0)".
- [x] D — implemented by Coco (awaiting independent acceptance; NOT mission
      closure): transactional Classic batch lifecycle, all production
      full/partial/flush/reproject completion paths through one transactional
      helper; real batch1 success/batch2 failure/on-disk resume witness;
      truthful fatal status. Evidence below in "Stage D evidence (Coco r0)".
- [x] E1 — implemented by Coco (stage E1 only: automatic CPU memory policy
      engine + production wiring; GUI/UX field removal, Boring-route
      migration and read-only status display are stage E2, NOT done here).
      Evidence below in "Stage E1 evidence (Coco r0)".
- [x] E2 — implemented by Coco (GUI/UX migration: HQ RAM Limit removed,
      read-only AUTO CPU-memory status, legacy value migration, explicit
      override stays env/CLI-only). Evidence below in
      "Stage E2 evidence (Coco r0)".
- [ ] F — version 8.4.0 everywhere authoritative, early CLI --version, wheel check,
      CHANGELOG beta/candidate, Windows incident/W80 instructions.
- [ ] All required targeted suites + final full-suite baseline differential.
- [ ] Junior final ACCEPT + Nono adversarial final ACCEPT.
- [ ] Windows incident witness performed by available Windows operator.
- [ ] W80 C/D JSONL and all A/B/C/D memory provenance; numerical+visual acceptance.

## Evidence

`docs/8.4.0_archaeology.md`; external durable evidence:
`/home/tristan/.openclaw/workspace/.a2a-reports/zsss-840-prew80-20260907/`.

## Acceptance detail (do not drop through context resets)

A: real FITS per accepted/fallback normalization; geometric/effective/overlap
fractions/counts; estimator+reason; sky offset / linear a,b; bounded allowlist,
append-only per run, no masks/M/images, I/O fail-open, SCI/WHT identical.
B/E: AUTO != strategy; legacy 16 GB never overrides GUI AUTO incl. subprocess;
expert override explicit+logged; reserve named/documented/tested; CPU ceiling
intersect runtime safe RAM; preflight independent from GPU name/VRAM, no physical
preallocation or future RAM promise. Exact budget reaches core; no 1/2GB fallback.
C: full/spatial SCI+WHT bitwise preferred; mono/RGB, weighted/unweighted,
full/partial support, NaNs, zeros, rank0/rank>0, rewinsor on/off, odd/even/partial
N; spies assert original N on every tile. 2/8/16/24GiB simulation, rectangular
fallback/minimum tile/refusal/bounded retry. Canonical science + global iteration
stop preserved. No raw-FITS reload or tile-local normalization. Real isolated
RSS native allocations and full/tiled time+peak/model comparison, not tracemalloc.
D: inject FULL_CPU/SPATIAL/GPU->CPU/minimum refusal/generic exception; no failed
source movement/counter/ledger/checkpoint/partial/cumulative advancement, terminal
FAILED; batch1 persists and batch2 physically remains retryable. Also check commit
error handling; preserve existing dirty fail-closed Resume contract.
F: Phase1, alignment, Winsor CPU/GPU/planner/provenance, HSI, Resume/checkpoint/
stacked, Qt/settings/preflight, Drizzle/COV and full-suite differential; diff-check;
exact local commits; Nono must find NONE for N-subdivision and failed-consumed
production paths. W80 fresh D:\tulip\W80_v1, pinned 1268 reference, N20,
kappa3/3 winsor .05/.05, A legacy sky B none C support sky D support linear;
AUTO, no forced tiling for normalization comparison. No visual-solvability claims.

## Debt / risks

Generic nonlinear HQ subgrouping and earlier weighting naming/dispatch debt stay
separate. CPU tiling requires global stop coordination (NOT naive independent
per-tile calls). Exported
stack_disk_streaming Winsor seam also has missing budget/local-stop and read-
error dropping; close or explicit fail-closed guard, no silent exclusion.
Original Phase1 state file contains historical checklist sections; latest closure
at its end is authoritative for prior Phase1, this file owns 8.4.0 progress.

## Stage B evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after independent
verification). No production execution path, reducer, queue_manager, GPU
planner or science modified — stage B is new files only.

Changes (worktree only, all NEW files):

* `seestar/core/cpu_memory_planner.py` — pure/testable CPU memory planner
  (no device access, no psutil at import, no GPU logic; all memory state
  injected as plain integers). Vocabulary: MODE_AUTO/OVERRIDE; strategy
  FULL_CPU / SPATIAL_TILED_CPU / CPU_MEMORY_REFUSAL; frozen
  `CpuMemoryDecision` with tile_shape (full-width bands first, rectangular
  only when a full-width band cannot fit), n_tiles, tile_outputs, minimum
  tile, estimated/per-tile peak bytes, effective budget, reserve, refusal
  reason. Structural exact-N: the decision carries input N verbatim and no
  field can encode a reduced/split N. Conservative measured envelopes:
  numpy slow 13.0x+16 MiB, numpy fast (rank0) 12.0x+16 MiB, scipy 17.5x+
  32 MiB; already-resident inputs counted once (never subtracted twice);
  named policy quantities CPU_POOL_WORKER_OVERHEAD_BYTES (448 MiB per extra
  worker) and CPU_OUTPUT_SERIALIZATION_FRAMES (2x frame bytes); documented
  reserve policy (fixed min 256 MiB + 2% proportional + pool/output
  quantities) via `recommended_reserve_bytes`; pure rank-regime detector
  `winsor_zero_rank_regime` (floor(limit*N)==0, mirrors canonical math);
  min viable tile CPU_MIN_TILE_OUT=96; effective_budget =
  min(ceiling, available - reserve).
* `tests/test_cpu_memory_planner.py` (NEW, 40 tests) — 2/8/16/24 GiB
  deterministic simulations at fixed N (N invariant; only strategy/geometry
  differs); structural exact-N (verbatim N, no N-split field); rank-0 vs
  rank>0 detection incl. weighted/unweighted and planner model selection;
  full-width band first vs rectangular fallback vs truthful minimum-tile
  refusal; reserve named/never-negative and effective-budget formula;
  model conservatism against every committed evidence row (21 fixtures);
  planner purity (subprocess import asserts no psutil); bounded retry
  vocabulary (tile_h/tile_w only; forbidden retry fields absent); frozen
  decision; OVERRIDE ceiling respected; invalid inputs raise.
* `tests/cpu_winsor_memory_profiler.py` (NEW) — reproducible isolated-
  subprocess RSS profiler of the canonical
  `stack_methods._stack_winsorized_sigma_iter` (resource.getrusage peak RSS
  + psutil watcher cross-check; no tracemalloc-only).

Profiling evidence (committed under mission evidence dir):
`/home/tristan/.openclaw/workspace/.a2a-reports/zsss-840-prew80-20260907/stage-b-evidence/`
(`cpu_winsor_rss_rows.json` 21 rows, `cpu_winsor_rss_evidence.md`).
Backends: python 3.13.5, numpy 2.5.2, scipy 1.18.1, bottleneck 1.6.0
(installed; NANMEAN/NANSTD via bottleneck), psutil 7.2.2, Linux.
Rows cover mono/RGB, N 10/20/36/50, full-frame/band/rect spatial, rank-0 vs
rank>0, apply_rewinsor T/F, weighted/unweighted, numpy default + one scipy
opt-in row. Measured deltas 2.3-582.3 MiB over resident input; numpy ratios
11.2-14.5x, scipy row 16.6x. Model margins all >= 0 (min margin 15.77 MiB).

Validation (repo `.venv/bin/python`, QT offscreen):

* `pytest tests/test_cpu_memory_planner.py -q`: 40 passed / 0 failed.
* `py_compile` on planner + both test/helper files: OK.
* `git diff --check`: clean (only 3 new untracked files).
* Full suite NOT run (Junior owns baseline/final differential).
* No production file modified in stage B.

## Stage C evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after verification).
Production wiring into `_stack_batch` dispatch intentionally NOT done (stage E
owns AUTO policy wiring; stage D owns transactional lifecycle).

Changes (worktree only):

* `seestar/core/stack_methods.py` — budget contract + neutral schedule seam:
  `_stack_winsorized_sigma_iter` default `max_mem_bytes=None` (resolves to the
  documented env/2 GiB standalone default ONLY for non-production callers;
  the production queue path always threads an explicit budget); extracted
  `_winsor_schedule_kappas` and the reusable per-iteration helper
  `_winsorized_sigma_iteration_body` (winsorize -> NANMEAN/NANSTD with the
  `n_valid_col<=1 -> sigma=0` rule -> low/high -> mask update -> n_rej); FULL
  loop now consumes the schedule via the helper with its historical
  early-exit/decay semantics preserved. Added pure `_winsor_zero_rank_regime`
  (local equivalent of the planner detector). FULL_CPU output is
  bitwise-identical to the pre-refactor implementation (legacy-equivalence
  tests + all existing CPU Winsorized suites green).
* `seestar/queuep/queue_manager.py` — `_stack_worker` args tuple gains the
  explicit 9th `max_mem_bytes` field and forwards it VERBATIM to core
  `_stack_winsorized_sigma`; legacy 8-field tuples (non-production callers)
  tolerated via documented `None` default; queue Winsorized wrapper resolves
  the budget ONCE (explicit arg wins; env/1 GiB fallback only when a
  non-production caller omits it) and threads it through the tuple for BOTH
  the direct seam and the `ProcessPoolExecutor` submit.
* `seestar/core/cpu_winsor_exact_n.py` (NEW) — exact-N SPATIAL CPU tiled
  driver `stack_winsorized_sigma_cpu_tiled` using the SAME canonical
  per-iteration helper / `_winsorize_bounds` / NANMEAN / NANSTD / kappa
  schedule as FULL_CPU. Proven two-pass global-iteration coordination:
  pass 1 runs the deterministic kappa schedule on every tile with NO early
  exit and sums LOCAL per-iteration rejection counts; `z_eff` = first global
  zero-rejection iteration (or max_iters; max_iters==1 -> 1); pass 2 replays
  exactly `z_eff` iterations per tile, finalises the survivor mask,
  `apply_rewinsor` per-column over the tile's own survivors, weighted/
  unweighted final reduction and EXACT PLACEMENT. Global rejected_pct is the
  sum/sum formula over tiles. Tile geometry: full-width bands `(tile_h,)`
  preferred, rectangular `(tile_h, tile_w)` fallback; full-frame geometry
  delegates to the untiled twin. Refusal semantics via
  `CpuWinsorMemoryRefused` reusing planner reasons (`cpu_budget_negative` /
  `cpu_no_valid_tile` / `cpu_min_tile_exceeds_budget`) and
  `CPU_MIN_TILE_OUT=96`; conservative model preflight when a budget is given;
  bounded spatial-only retry on catchable MemoryError (smaller tile_h/tile_w
  only — N / reducer / kappa / winsor / normalization / weights are never
  retry-mutable). Tile-order invariance hook `_tile_order` (rowmajor /
  reversed) mirrors the GPU driver.
* `tests/test_cpu_winsor_budget.py` (NEW, 7 tests) — sentinel regression
  (outer resolved `25769803949` 24 GiB+173 must reach the core boundary
  exactly, not the historical `2e9`); queue wrapper threads the resolved
  budget through the 9-field tuple; discriminating core-guard test (small
  resolved budget -> deep MemoryError in the direct seam AND through a REAL
  spawn `ProcessPoolExecutor`); direct-vs-pool identical contract on
  success; legacy 8-field tolerance maps to documented standalone default;
  wrapper guard uses the resolved budget.
* `tests/test_cpu_winsor_exact_n.py` (NEW, ~26 tests) — schedule seam vs
  reference sequence; refactor before/after equivalence (legacy reference
  embedded from committed pre-stage-C source; bitwise SCI/WHT/pct across
  mono/RGB x odd/even N x limits x rewinsor x decay); FULL vs TILED bitwise
  parity matrix (mono/RGB x weighted/unweighted x NaN/zero x rank0/rank>0 x
  rewinsor T/F x odd/even N x partial final band/rect) with per-iteration
  spies asserting tile N == original N on every canonical invocation;
  tile-order invariance; partial-frame exactness; untiled delegation;
  refusal (below-min tile, budget preflight, no-empty-success); bounded
  spatial retry success + truthful refusal; adversarial witnesses: global
  Winsorized(N=20 `[0]*19+[100]`) SCI 0 / W 20 vs subgroup composition SCI 5
  (intentionally divergent) and exact-N tiled == global on the same case;
  two-region mild+strong global-stop coordination == FULL bitwise.

Validation (repo `.venv/bin/python`, QT offscreen):

* `pytest tests/test_cpu_winsor_budget.py tests/test_cpu_winsor_exact_n.py -q`
  -> 79 passed / 0 failed.
* `pytest tests/test_winsorized_sigma.py tests/test_rewinsorization.py tests/test_hsi_closure_rejection.py -q`
  -> 18 passed / 0 failed.
* `pytest tests/test_hierarchical_stacking_integrity.py tests/test_drizzle_semantics_d2.py -q`
  -> 46 passed / 0 failed (directly exercise the refactored CPU winsor path).
* `py_compile` on all changed/new files: OK. `git diff --check`: clean.
* Full suite NOT run (Junior owns baseline/final differential).
* No `_stack_batch` dispatch / GUI / version / GPU planner / Drizzle / COV
  science modified in stage C.

### Stage D REWORK-1 (Coco r1)

Junior defect: `_combine_batch_result(..., strict_commit=True)` still had SIX
silent early-return failure paths that did not raise, so the transactional
helper treated them as success (counter advanced, sources moved / partial /
count-file / meta despite no SUM/WHT commit).

Fixed (all in `seestar/queuep/queue_manager.py`, `_combine_batch_result`):
under `strict_commit=True` the six paths now set `processing_error` +
`stop_processing` and RAISE `BatchReductionError` with a truthful reason:
coverage shape mismatch, colour image shape mismatch, gray image shape
mismatch, unexpected gray dimensions, near-zero coverage, non-finite /
zero-weight batch sum. The two paths inside the accumulation `try` re-raise
unchanged through the generic tail handler (added `isinstance(e,
BatchReductionError)` guard: no double count / double wrap).
`strict_commit=False` default behaviour byte-identical (silent return +
`failed_stack_count` increment) for out-of-scope callers. No SUM/WHT science
change.

Tests added to `tests/test_stage_d_transactional_classic.py` (18 new): for
each of the six paths — direct `_combine_batch_result(..., strict_commit=True)`
raises with terminal state set and failed frames counted once; via
`_process_completed_batch` no `_move_to_stacked` / `_save_partial_stack` /
`_update_batch_count_file` / `_update_batches_meta` (spy-asserted zero
calls), committed counter rolled back, no count-file advance, sources
physically in place, cumulative SUM/WHT unchanged, `stop_processing` True,
`processing_error` set; `strict_commit=False` legacy silent behaviour
preserved.

Validation: `pytest tests/test_stage_d_transactional_classic.py -q` ->
38 passed (20 r0 + 18 r1). `pytest tests/test_queue_manager_reproject.py
tests/test_coverage_support_classic.py tests/test_zsss_lifecycle_reliability.py
tests/test_reliability_source_immutability_r1.py tests/test_exposure_metadata_contract.py
tests/test_interbatch_classic.py -q` -> 138 passed / 5 failed — the 5 are
EXACTLY the documented inherited failures (inventory lines 3-5 & 15-16),
no new failures. `py_compile` OK; `git diff --check` clean. Full suite NOT
run. No commit.

## Stage D evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after independent
verification). Stage E owns AUTO-policy dispatch wiring (planner/tiled driver
into `_stack_batch`); NOT wired here.

Changes (worktree only, all in `seestar/queuep/queue_manager.py` + one NEW
production test file):

* `BatchReductionError(RuntimeError)` (NEW) — truthful dedicated failure signal
  with stable `reason` and optional `cause`.
* `_stack_batch` — a genuine reducer failure (FULL_CPU / SPATIAL_TILED_CPU /
  GPU->CPU fallback / minimum-tile CPU refusal / MemoryError / ANY reducer
  exception) now RAISES `BatchReductionError` instead of returning the
  ambiguous `(None,None,None)`. Empty batch / all-items-filtered / reproject
  missing-WCS stay distinct NON-fatal no-reducer-ran `(None,None,None)`
  conditions.
* `_combine_batch_result` — new `strict_commit=False` kwarg (default preserves
  the historical swallow-and-stop for out-of-scope callers: Drizzle cached
  reprojection and direct unit calls). Under `strict_commit=True` every commit
  failure (checkpoint persist, MemoryError, generic accumulation, missing
  memmaps, missing stacked data) sets the terminal failure state and RAISES
  `BatchReductionError`. SUM/WHT science untouched.
* `_process_completed_batch` — now the SINGLE transactional completion helper
  for every Classic full-batch / final-partial / flush / (mechanically)
  reproject completion caller (worker classic trigger, worker final-partial
  classic, `_flush_current_batch` batch-plan flush, reproject in-loop flush,
  reproject final partial — all five sites rewired). Ordering: REDUCE
  (reducer failure propagates) -> persist+solve for reproject paths -> COMMIT
  via `_combine_batch_result(..., strict_commit=True)` with the committed
  counter advanced EXACTLY ONCE immediately before the durable accumulation +
  resume checkpoint (rolled back on commit failure) -> ONLY AFTER the durable
  commit: `_move_to_stacked`, `_save_partial_stack`,
  `_update_batch_count_file`, `_update_batches_meta`, path/ledger clears.
  Returns True (committed) / False (non-fatal no-commit: empty batch, no
  reducer output, unsolved reproject skip — nothing consumed/advanced,
  batch left retryable); raises `BatchReductionError` on reduction/commit
  failure. Legacy tail cleanup factored into `_classic_batch_epilogue`.
* Worker reproject in-loop flush keeps its historical no-mid-run-move policy
  (sources consumed at the final partial flush); unsolved-reproject combine
  skip is a non-fatal no-commit (batch retryable) instead of the historical
  consume-without-accumulate. No Reproject photometry / science change.
* `tests/test_stage_d_transactional_classic.py` (NEW, 20 tests) — failure
  matrix (FULL_CPU MemoryError / SPATIAL_TILED_CPU MemoryError / GPU->CPU
  fallback reducer error / minimum-tile `CpuWinsorMemoryRefused` / generic
  reducer exception + raw escape rows): per row no `_move_to_stacked`, no
  committed-counter advance, no count-file/meta advance, no checkpoint/ledger
  advance, no partial-cumulative mutation, truthful raise; real-pipeline
  conversion row (in-`_stack_batch` MemoryError -> `BatchReductionError`);
  success-path control (commit then consume); counter == committed (never
  attempted) after mid-run failure; on-disk resume witness (batch 1 under
  `stacked/`, count file "1", batch-2 source physically in place, cumulative
  == exactly batch 1, re-run does NOT skip batch 2); empty and no-reducer-
  output batches non-fatal no-commit; `_combine_batch_result` strict failure
  signalling (checkpoint dirty-persist fail-before-mutation, memory,
  accumulation, direct BRE rollback) + legacy non-strict default unchanged;
  reproject-flavoured completion commit/failure rows.

Validation (repo `.venv/bin/python`, `QT_QPA_PLATFORM=offscreen`):

* `pytest tests/test_stage_d_transactional_classic.py -q` -> 20 passed.
* `pytest tests/test_stage_d_transactional_classic.py tests/test_queue_manager_reproject.py -q`
  -> 48 passed.
* `pytest tests/test_global_reference_audit.py tests/test_coverage_support_classic.py
  tests/test_zsss_lifecycle_reliability.py tests/test_reliability_source_immutability_r1.py -q`
  -> 78 passed / 2 failed — BOTH verified identical on the pristine pre-stage-D
  worktree (stash) and listed in the historical r2 inventory lines 15-16
  (`test_worker_minimal_run_keeps_source_when_move_stacked_false`,
  `test_worker_minimal_run_moves_source_by_default`) — inherited, not
  regressions.
* `pytest tests/test_exposure_metadata_contract.py tests/test_interbatch_classic.py
  tests/test_hierarchical_stacking_integrity.py -q` -> 81 passed / 3 failed —
  identical on pristine (stash); inventory lines 3-5
  (`test_drizzle_failed_add_increments_neither`,
  `test_drizzle_post_admission_side_effect_failure_keeps_lockstep`,
  `test_resume_continuation_no_double_count`) — inherited.
* `pytest tests/test_resume.py -q` -> 156 passed / 4 failed — the 4 documented
  inherited failures (same names as stage A) — no new failures.
* `pytest tests/test_single_batch_csv.py tests/test_stacked_filter_relative.py
  tests/test_reliability_stacked_restore_b.py tests/test_resume_intent_contract_rsm2.py -q`
  -> 27 passed.
* `py_compile` on changed/new files: OK. `git diff --check`: clean.
* Full suite NOT run (Junior owns baseline/final differential).

## Stage E1 evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after independent
verification). Stage E1 only: automatic CPU memory policy engine + production
wiring. GUI/UX field removal, Boring-route migration and read-only status
display are stage E2 (NOT done here). Version bump/changelog are stage F.

Changes (worktree only):

* `seestar/core/cpu_memory_policy.py` (NEW, pure) — policy resolution +
  provenance record builders, no device access, no psutil import, all memory
  state injected as plain integers. `CpuMemoryPolicyPreflight` +
  `resolve_cpu_policy_preflight` (AUTO ceiling =
  max(0, available_ram_preflight - reserve); OVERRIDE ceiling = explicit
  requested budget, provenance-visible); `resolve_cpu_winsor_decision`
  (delegates to the stage-B `plan_cpu_winsor_execution`, runtime
  re-evaluation effective_budget = min(policy_ceiling, available_now -
  reserve) with the named stage-B reserve — fixed min 256 MiB + 2%
  proportional + explicit pool/output quantities, never an unexplained
  magic number); record builders for the four event kinds (MEMORY_POLICY /
  CPU_WINSOR_MEMORY_DECISION / CPU_WINSOR_MEMORY_RETRY /
  CPU_WINSOR_MEMORY_REFUSAL) — plain JSON-safe dicts, no image/mask arrays,
  bounded. Env override seam documented: `ZSSS_CPU_MEMORY_OVERRIDE_BYTES`.
* `seestar/queuep/queue_manager.py` — production wiring:
  - durable/throttle-exempt provenance prefixes added for MEMORY_POLICY and
    the three CPU_WINSOR_MEMORY_* blocks;
  - RAM providers with deterministic injectable overrides
    (`_cpu_total_ram_bytes` / `_cpu_available_ram_bytes_now` /
    `_cpu_process_rss_bytes`, psutil-backed) and the explicit override seam
    (`_cpu_memory_override_bytes`: internal attr first, then the env var —
    the legacy `max_hq_mem` is NEVER consulted);
  - `_capture_cpu_memory_policy_preflight` — once-per-accepted-run POLICY
    capture (mode / total+available RAM / reserve / ceiling / process RSS /
    GPU requested+effective backend / cpu_fallback_viable) emitting the
    durable MEMORY_POLICY record; fail-open; establishes POLICY+capability
    only, never a frozen promise; CPU RAM never derived from GPU
    model/VRAM/CUDA name; hooked in `start_processing` right after the
    accepted-run seam (`_reset_gpu_execution_telemetry`); lazy capture for
    direct-dispatch harnesses;
  - `_run_cpu_winsor_policy` — the runtime CPU-execution helper: re-reads
    available RAM now, recomputes the named reserve, resolves the decision
    via the pure module, emits CPU_WINSOR_MEMORY_DECISION (+ REFUSAL on a
    refusal; + RETRY record when the tiled driver's bounded spatial
    allocation retries are exhausted, details.attempts >= 2), dispatches
    FULL_CPU -> `_stack_winsorized_sigma` with the explicit resolved budget /
    SPATIAL_TILED_CPU -> stage-C `stack_winsorized_sigma_cpu_tiled` with the
    planner tile_shape + explicit budget / CPU_MEMORY_REFUSAL -> raises
    `CpuWinsorMemoryRefused` (stage D -> truthful terminal FAILED);
  - `_stack_batch` Winsorized branch: the legacy
    `use_tile_mode = total_bytes > self.max_hq_mem` +
    `_combine_hq_by_tiles` N-subgroup heuristic is REMOVED for Winsorized;
    the `_cpu_winsorized` closure now resolves the automatic CPU policy
    (same closure is the GPU CPU_FALLBACK target of
    `_gpu_reduce_winsorized` — no hidden legacy memory defaults, no
    scientific-N subdivision, GPU admission execution=eligible semantics
    preserved). Generic non-Winsorized `_combine_hq_by_tiles` callers
    (median / kappa-sigma / linear-fit-clip) unchanged — documented debt.
* `tests/test_cpu_memory_policy.py` (NEW, 17 tests) — pure module: 2/8/16/24
  GiB RAM simulations at fixed N=36 (N invariant; 16/24 FULL, 2/8 SPATIAL
  with strictly larger tiles at 8 GiB; effective budget monotone; reserve
  never-negative) + sub-min-tile refusal; AUTO ceiling from
  available-minus-reserve; OVERRIDE ceiling = requested budget and
  provenance-visible (env + internal seam); runtime re-evaluation
  effective = min(ceiling, available - reserve) incl. ceiling binding when
  RAM grew; record builders JSON-safe/bounded/no arrays with correct
  strategy tokens. Production wiring (real `_stack_batch` Winsorized on a
  bare stacker, real `_gpu_reduce_winsorized`, real
  `_process_completed_batch`): preflight MEMORY_POLICY emitted once with
  override tokens; AUTO ignores legacy max_hq_mem; FULL_CPU wired budget
  reaches the worker tuple byte-for-byte (stage C sentinel 24 GiB + 173 —
  no 1/2 GiB default, legacy max_hq_mem=42 poison never consulted);
  SPATIAL_TILED_CPU calls the exact-N tiled driver with the planner
  tile_shape and explicit budget; refusal raises BatchReductionError through
  real `_stack_batch` and through `_process_completed_batch` consumes
  nothing (no source move / counter / count-file / cumulative); GPU
  CPU_FALLBACK (GPU planner mocked to CPU_FALLBACK on a real cupy device
  host) routes through the automatic CPU policy closure; provenance
  emission fail-open (raising record builder never breaks capture or the
  run); per-decision records bounded.

Validation (repo `.venv/bin/python`, `QT_QPA_PLATFORM=offscreen`):

* `pytest tests/test_cpu_memory_policy.py -q` -> 17 passed.
* `pytest tests/test_cpu_memory_planner.py tests/test_cpu_winsor_budget.py
  tests/test_cpu_winsor_exact_n.py tests/test_winsorized_sigma.py
  tests/test_rewinsorization.py -q` -> 122 passed (0 failed).
* `pytest tests/test_gpu_pool_release.py tests/test_hierarchical_stacking_integrity.py
  tests/test_drizzle_semantics_d2.py tests/test_stage_d_transactional_classic.py -q`
  -> 95 passed (0 failed) — real-cupy GPU wiring tests ran on this host
  (cupy 14.2, 1 device).
* `py_compile` on changed/new files: OK. `git diff --check`: clean.
* Full suite NOT run (Junior owns baseline/final differential).

Uncertainties / limits:

* CPU_WINSOR_MEMORY_RETRY is emitted only at the observable wiring boundary
  (bounded allocation retries exhausted inside the stage-C tiled driver,
  attempts >= 2 in the refusal details); successful driver-internal retries
  are not observable without modifying the driver (prohibited) and remain
  covered by the stage-C bounded-retry tests.
* AUTO ceiling uses the preflight available RAM minus the preflight reserve;
  the runtime effective budget recomputes the reserve from the RAM available
  at execution time (recorded policy fields are capability, not promise).
* Existing non-E1 tests that drive the real Winsorized CPU `_stack_batch`
  without RAM overrides now evaluate AUTO against real psutil RAM
  (test_gpu_pool_release CPU-by-policy rows): on this host they pass (FULL);
  on a machine with < ~2 GiB free they would plan SPATIAL (bitwise-identical
  results per stage C) or truthfully refuse below the minimum viable tile.

## Stage E2 evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after independent
verification). GUI/UX migration only — the E1 automatic CPU policy engine,
planner, reducer, tiled driver, stage C/D wiring, Drizzle, COV, version and
science are untouched. Stage F (version/changelog) still pending.

Changes (worktree only):

* `seestar/gui_qt/main_window.py` — the editable "HQ RAM Limit (GB)" QSpinBox
  is REMOVED (attribute deleted, not kept disabled) and replaced by a
  read-only status row: label key `cpu_memory_policy` + static QLabel
  `cpu_memory_status_label` ("Automatic (re-evaluated at execution)"), bound
  for FR/EN language refresh. No new tuning knob. Signal connection, control
  collection (`_sync_state_from_controls`), control sync list + setValue,
  boring-launch `max_mem_gb=` forwarding and the run-request
  `max_hq_mem_gb=` seam attachment are all removed.
* `seestar/gui_qt/localization.py` — new FR/EN entries for
  `cpu_memory_policy` and `cpu_memory_auto_status` (old `hq_ram_limit` entry
  kept for backward compatibility).
* `seestar/gui_qt/run_handoff.py` — `max_hq_mem_gb` removed from
  `QT_SEAM_FIELDS` and from `attach_run_settings` (seam set is now
  `use_gpu` + `reference_origin_hint`); docstrings updated.
* `seestar/gui_qt/backend_runner.py` — `_apply_seam_kwargs` no longer converts
  `max_hq_mem_gb` into `stacker.max_hq_mem` (legacy value never applied to
  the engine instance); docstring updated.
* `seestar/gui_qt/settings_state.py` — `max_hq_mem_gb` field retained ONLY as
  a deprecated migration/diagnostics carrier (comment updated; never
  forwarded).
* `seestar/gui_qt/boring_route.py` — `max_mem_gb` default becomes `None`;
  `--max-mem` is OMITTED for normal boring runs (AUTO policy in the
  subprocess); an explicit `max_mem_gb=` still emits `--max-mem` (expert
  override path).
* `seestar/gui/main_window.py` (legacy Tk) — HQ RAM label + spinbox row
  removed; related tooltip bindings and the tr-label mapping removed; the
  boring subprocess command no longer forwards `--max-mem` from the legacy
  settings value.
* `seestar/gui/settings.py` — functionally unchanged (legacy attribute
  persists for deterministic migration/diagnostics; AUTO engine never
  consumes it — verified by the regression tests).
* `seestar/gui/boring_stack.py` — an EXPLICIT `--max-mem` (default None) now
  also sets `ZSSS_CPU_MEMORY_OVERRIDE_BYTES` (provenance-visible OVERRIDE
  seam) in addition to the legacy `SEESTAR_MAX_MEM`; help text updated.
* Tests (deliberate migrations, not hidden failures):
  `tests/test_qt_stacking_m16.py` (spin absence + AUTO status label + no new
  knob; new localization keys; engine-coupled wiring now asserts
  `max_hq_mem_gb` is NOT forwarded), `tests/test_qt_run_settings_handoff_m20.py`
  (seam set excludes `max_hq_mem_gb`; backend no longer applies legacy mem),
  `tests/test_qt_boring_mem_m25.py` (normal boring omits `--max-mem`; env
  override never leaks into argv; explicit override still forwards).
* `tests/test_qt_cpu_memory_policy_migration.py` (NEW, 7 tests) — persisted
  legacy "HQ RAM Limit = 16 GB" in the settings model / engine attribute does
  NOT become the AUTO runtime budget (policy record only, no multi-GiB
  allocation); no editable HQ RAM field + AUTO status present + no new
  tuning knob; run request / backend carry no legacy mem fields; env override
  `ZSSS_CPU_MEMORY_OVERRIDE_BYTES` still provenance-visible (mode=OVERRIDE,
  requested_budget_bytes) and never surfaces as a GUI knob.

Validation (repo `.venv/bin/python`, `QT_QPA_PLATFORM=offscreen`):

* `pytest tests/test_qt_run_settings_handoff_m20.py tests/test_qt_boring_mem_m25.py
  tests/test_qt_boring_route.py tests/test_qt_stacking_m16.py -q` -> 65 passed.
* `pytest tests/test_qt_cpu_memory_policy_migration.py -q` -> 7 passed.
* `pytest tests/test_qt_backend_activation.py tests/test_qt_batch_spinner_phaseB2.py
  tests/test_qt_run_outcome_lifecycle.py tests/test_qt_progress_log.py
  tests/test_run_config_classic_parity.py -q` -> 73 passed.
* `pytest tests/test_qt_settings_state.py tests/test_qt_settings_persistence.py
  tests/test_qt_persistence_window.py tests/test_qt_backend_runner.py
  tests/test_qt_localization.py tests/test_qt_expert_m15.py -q` -> 82 passed /
  2 failed — the 2 failures (`test_cov06b_modern_coverage_surface_and_backend_wiring`,
  `test_reset_expert_settings_restores_defaults`, coverage-render default)
  are PRE-EXISTING: verified byte-identical on the pristine HEAD 5413d491 via
  git stash. NOT E2 regressions.
* `py_compile` on all changed files: OK. `git diff --check`: clean.
* Full suite NOT run (Junior owns baseline/final differential).

## Current handoff

Phase1 anchor commit:76ac656a948e6e481540cb7d6e22c90014c29f14 (all accepted
files preserved, only historical extra EOF blank removed from new test).
Stage A ACCEPTED and committed at 9d3c1081f0873b076af3a9ad25f3a6cffb11687c;
Stage B ACCEPTED and committed at 580ec18e7e415a07da46d1049545d4c3cde95d0f;
Stage C ACCEPTED and committed at d1b21846e25abda48fad8e60319a1dd1f5acb3e5;
Stage D ACCEPTED (after REWORK-1) and committed at
551cd24f97122dc14d1721b633bcc843b3712730;
Stage E1 ACCEPTED and committed at 5413d4915ed89951145b431399b153ff86cc9966.
Coco main reset ONCE after observed status done, counters0; keep same session
for all 8.4.0 stages/reworks. Stage E2 implemented (Coco r0 report
`zsss-840-prew80-20260907.coco.e2.r0.md`), awaiting Junior independent
acceptance; do not reset Coco or rerun full suites per stage. Next:
stage F (version 8.4.0 everywhere authoritative, early CLI --version, wheel
check, CHANGELOG beta/candidate, Windows incident/W80 instructions) after
acceptance.

## Stage A evidence (Coco r0)

Implementation status: DONE (worker completion is NOT architect acceptance and
NOT mission closure). No local commit (Junior commits after independent
verification).

Changes (worktree only):

* `seestar/core/normalization_diagnostics.py` (NEW) — versioned JSON-Lines
  per-run artifact `normalization_diagnostics_<session_token>.jsonl`;
  allowlisted bounded scalar records (frame + `_SRCFILE` evidence,
  normalization_method, status, reason/neutral_reason/estimator/
  estimator_reason, geometric/effective/overlap counts+fractions,
  sampled_estimator_count, canvas_area, sky_offset or bounded linear a/b
  channel vectors, schema_version/session_id/ts); finite JSON-safe numbers;
  `MAX_CHANNELS=3`, `MAX_RECORD_BYTES=8192`; append-only, never truncates;
  validation rejects unknown keys / nested payloads / arrays / non-finite /
  oversize; `append_record` fail-open (never raises).
* `seestar/queuep/queue_manager.py` — durable sink methods
  (`_p1_normalization_diagnostics_path`, `_p1_persist_normalization_diagnostics`,
  `_p1_header_srcname`); per-run session token rotated at each fresh reference
  capture and cleared at release (repeated starts/resume get distinct
  artifacts; prior evidence never overwritten); `_stack_batch` passes real
  accepted-header `_SRCFILE` basenames lockstep into
  `_normalize_sources_against_reference(..., source_names=...)`;
  per-frame durable records for every accepted/neutral sky_mean/linear_fit
  event (none stays strict no-op, no events); in-memory diagnostics bounded
  ring (`_P1_MEM_DIAG_MAX_ROWS`) with truthful aggregate accepted/neutral
  counters that survive eviction.
* `seestar/core/overlap_normalization.py` — diagnostic-only scalar
  `n_geometric` (eroded geometry-support pixel count) attached to estimator
  diag while the mask is alive; neutral diags carry explicit `None` (unknown)
  when geometry was never derived. No sample selection/math change.
* `tests/test_normalization_diagnostics.py` (NEW, 16 tests) — real basename in
  accepted sky_mean / accepted linear_fit / neutral fallback events; filtered
  bad item does not shift identity; append-within-run + distinct per-run
  artifacts on same stacker reuse; `none` no-op (no events); allowlist/
  bounded-record rejection of arrays/nested/non-finite/oversize/ragged
  channel vectors; writer-denied and serialization-failure fail-open with
  SCI/WHT bit-identical vs disabled and working sinks through the real
  `_stack_batch` seam; fraction/sample semantics; geometric count attached in
  estimators; in-memory ring bound with truthful summary.

Validation (repo `.venv/bin/python`, `QT_QPA_PLATFORM=offscreen`):

* `pytest tests/test_normalization_diagnostics.py -q`: 16 passed.
* `pytest tests/test_overlap_normalization_p1.py tests/test_p1_seam_witnesses.py -q`: 110 passed.
* `pytest tests/test_hsi_closure_normalization.py tests/test_hsi_closure_backend_parity.py -q`: 45 passed.
* `pytest tests/test_resume.py -q`: 156 passed / 4 failed — the 4 failures
  (`test_start_processing_valid_resume_pins_reference_and_queue`,
  `test_start_processing_quality_weighted_captures_q_ref_once_fresh`,
  `test_start_processing_quality_weighted_resume_skips_recomputation`,
  `test_repeated_start_binds_fresh_canonical_config`) reproduce identically on
  the pristine pre-stage-A worktree (verified by stash) and are listed in the
  historical r2 full-failure inventory
  (`inherited-phase1-r2-full-failures.txt`) — inherited, not regressions.
* End-to-end smoke through `_stack_batch` with real `_SRCFILE` headers:
  artifact created, two accepted events, frames attributed by real basename.
* `git diff --check`: clean.
* No full-suite run (Junior owns baseline/final differential).
